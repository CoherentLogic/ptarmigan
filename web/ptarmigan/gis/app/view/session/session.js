Ext.define('pt_gis.view.session.session', {
	extend: 'Ext.window.Window',
	alias: 'widget.viewsession',
	title: 'View Session',
	layout: 'fit',
	autoShow: true,
	
	initComponent: function () {
		this.items = [
		
		];
	}
});
